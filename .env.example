# Local development against LocalStack. Copy to .env and adjust per project if needed.
# Omitting AWS_ENDPOINT_URL in a deployed environment targets real AWS with the same code path.
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=eu-west-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
