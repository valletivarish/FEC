{
  "name": "gridpulse-backend",
  "version": "1.0.0",
  "description": "GridPulse Lambda handlers: telemetry ingest and hub/bay query APIs",
  "private": true,
  "scripts": {
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-kinesis": "^3.600.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.0.0",
    "jest": "^29.7.0",
    "eslint": "^9.17.0"
  }
}
