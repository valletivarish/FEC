Bootstrap scripts for LocalStack resources (queues, tables, streams) go here as
`NN-description.sh`, run in filename order by `make init-aws`. Each project adds
only the scripts it needs when its turn comes; nothing here is shared boilerplate.
