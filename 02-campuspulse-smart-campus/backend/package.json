{
  "name": "campuspulse-backend",
  "version": "1.0.0",
  "private": true,
  "description": "CampusPulse backend Lambda handlers (ingest, alerts, zone status/history)",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/util-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.0.0",
    "eslint": "^9.39.2",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
