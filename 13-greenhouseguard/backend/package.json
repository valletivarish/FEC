{
  "name": "greenhouseguard-backend",
  "version": "1.0.0",
  "private": true,
  "description": "GreenhouseGuard backend Lambda handlers",
  "scripts": {
    "test": "jest",
    "lint": "cd .. && node_modules/.bin/eslint backend"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.600.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.0.0",
    "jest": "^29.7.0",
    "eslint": "^10.6.0",
    "@eslint/js": "^10.0.1"
  }
}
