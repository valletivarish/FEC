{
  "name": "greenhouseguard-lint-root",
  "version": "1.0.0",
  "private": true,
  "description": "Holds eslint.config.js's own dependencies; each workspace stays independently installable",
  "devDependencies": {
    "eslint": "^10.6.0",
    "@eslint/js": "^10.0.1"
  }
}
