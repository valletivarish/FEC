{
  "name": "parkfog-backend",
  "version": "1.0.0",
  "private": true,
  "description": "ParkFog backend Lambda functions (ingestBayEvents, queryZoneStatus, relayBayEvents)",
  "scripts": {
    "test": "jest",
    "lint": "eslint --config ../eslint.config.js ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/client-sqs": "^3.1079.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0"
  },
  "devDependencies": {
    "aws-sdk-client-mock": "^4.0.0",
    "jest": "^29.7.0",
    "eslint": "^9.9.0",
    "@eslint/js": "^9.9.0"
  }
}
